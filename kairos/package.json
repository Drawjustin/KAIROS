{
  "name": "kairos-commitlint",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "commitlint": "commitlint --config commitlint.config.cjs --edit",
    "mock:openai": "node tools/load-test/mock-openai-provider.mjs"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.0",
    "husky": "^9.1.7"
  }
}
