apiVersion: 1

providers:
  - name: kairos
    orgId: 1
    folder: KAIROS
    type: file
    disableDeletion: false
    # 대시보드를 화면에서 고쳐도 파일이 원본이다. 재현 가능해야 산출물이라 할 수 있다.
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
